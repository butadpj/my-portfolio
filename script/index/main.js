import '../scrollToTop';
import '../navbar';
import '../collapsible';
import '../parallax';
import Butter from 'butter-es6';

Butter.init({
  wrapperId: 'butter',
  cancelOnTouch: true,
  wrapperDamper: 0.05,
});
