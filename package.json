{
  "devDependencies": {
    "copy-webpack-plugin": "^10.2.4",
    "husky": "^7.0.4",
    "webpack": "^5.70.0",
    "webpack-cli": "^4.9.2",
    "webpack-dev-server": "^4.7.4",
    "webpack-merge": "^5.8.0"
  },
  "name": "my-portfolio",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "webpack serve --config webpack.dev.js",
    "build": "webpack --config webpack.prod.js",
    "prod": "npm run build",
    "postinstall": "node ./node_modules/husky/lib/installer/bin install",
    "deploy": "git config --global user.email 'butadpj@gmail.com' & git config --global user.name 'butadpj' & git push origin `git subtree add --prefix build master`:github-pages --force"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.0.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run prod"
    }
  }
}
